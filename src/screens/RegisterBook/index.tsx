import { Text, View } from 'react-native';
import { styles } from './styles';
import { Book } from '../../models/Book';

export function RegisterBookScreen() {
	return (
		<View style={styles.container}>
			<Text style={styles.title}>Book 1</Text>
		</View>
	);
}

