import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	input: {
		backgroundColor: 'white',
		borderColor: '#aaa',
		borderRadius: 4,
		elevation: 5,
		padding: 12,
	},
	error: {
		color: 'red',
	},
});

